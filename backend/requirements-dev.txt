# =============================================================================
# requirements-dev.txt — Test-only dependencies (never deployed to production)
# =============================================================================
# Install: pip install -r requirements-dev.txt
# These sit ON TOP OF requirements.txt (which has FastAPI, SQLAlchemy, etc.)
# =============================================================================

pytest>=8.0.0                    # Test runner
pytest-asyncio>=0.23.0           # CRITICAL: without this, async def tests silently pass without running
pytest-cov>=5.0.0                # Coverage reports
httpx>=0.27.0                    # TestClient dependency for newer FastAPI/Starlette
moto[ec2,cloudwatch,ce]>=5.0.0   # Intercept boto3 calls at the botocore transport layer
